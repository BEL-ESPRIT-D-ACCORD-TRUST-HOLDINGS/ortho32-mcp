{
  "name": "ortho32-mcp",
  "version": "0.1.0",
  "description": "Deterministic stdio MCP server implemented in AWK.",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "ortho32-mcp": "bin/ortho32-mcp.awk"
  },
  "scripts": {
    "start": "awk -f bin/ortho32-mcp.awk",
    "test": "node tests/protocol.test.mjs",
    "lint": "node tests/protocol.test.mjs"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

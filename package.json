{
  "name": "ortho32-mcp",
  "version": "0.1.0",
  "description": "ORTHO32 MCP Server — CLIENT of ORTHO services via ORTHOServiceClient -> ortho32-api HTTP. Not a backend.",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "ortho32-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
